import * as React from 'react';

const OffLine = () => (
  <div className="splash-container">
    <div className="splash server-offline-msg">
      <span className="text">Sorry, but the server is offline</span>
    </div>
  </div>
);

export default OffLine;
